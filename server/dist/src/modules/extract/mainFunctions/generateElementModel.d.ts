import type { ElementModel } from "../../../../types/extract";
declare const generateElementModel: (element: any, links: ElementModel[], images: ElementModel[], url: string, parents?: string[]) => ElementModel | ElementModel[];
export default generateElementModel;
